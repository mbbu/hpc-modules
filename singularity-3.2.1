the following packages need to be available in the system
$ sudo yum update -y && \
    sudo yum groupinstall -y 'Development Tools' && \
    sudo yum install -y \
    openssl-devel \
    libuuid-devel \
    libseccomp-devel \
    wget \
    squashfs-tool
    
    install GO
    export VERSION=1.11 OS=linux ARCH=amd64 && \###change to current version
    wget https://dl.google.com/go/go$VERSION.$OS-$ARCH.tar.gz && \
    sudo tar -C /usr/local -xzvf go$VERSION.$OS-$ARCH.tar.gz && \
    rm go$VERSION.$OS-$ARCH.tar.gz
    
    set up enviroment 
    $ echo 'export GOPATH=${HOME}/go' >> ~/.bashrc && \
    echo 'export PATH=/usr/local/go/bin:${PATH}:${GOPATH}/bin' >> ~/.bashrc && \
    source ~/.bashrc
    
    $ go get -u github.com/golang/dep/cmd/dep
    
    in hpc we developed/build from source
    export VERSION=3.0.3 && # adjust this as necessary \
    mkdir -p $GOPATH/src/github.com/sylabs && \
    cd $GOPATH/src/github.com/sylabs && \
    wget https://github.com/sylabs/singularity/releases/download/v${VERSION}/singularity-${VERSION}.tar.gz && \
    tar -xzf singularity-${VERSION}.tar.gz && \
    cd ./singularity && \
    ./mconfig
    
    ##follow the steps one by one to correct errors
    
    compile 
    ./mconfig && \
    make -C ./builddir && \
    sudo make -C ./builddir instal
    
    ##for shared resource set the build location that is accessible by all users
    
   ./mconfig --prefix=/opt/singularity
   
   Source bash completion file
   
   $ . /usr/local/etc/bash_completion.d/singularity
    
    
###use singularity commands to build and download imagess
usage
##use singularity on the terminal to start the instance
